# Machinery index

What runs, where it lives, and what triggers it. This file sits inside the tree it
describes, not above it: a list of what runs can be false, which disqualifies it from
`00-core/`. Nothing here is auto-loaded: a human names it in the request or it stays
out of the session.

| What | Path | Trigger | Writes to |
|---|---|---|---|
